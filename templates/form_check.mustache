<div class="form-check">
  <input class="form-check-input" type="{{type}}" id="{{id}}"
         name="{{name}}" value="{{value}}"
         {{#onchange}}onchange="{{onchange}}"{{/onchange}}>
  <label class="form-check-label {{color}}" for="{{id}}">
    {{label}}
  </label>
</div>